<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.td.admin.dao.CancelOrderDao">




   <insert id="add" parameterType="map">
       INSERT INTO
          t_cancel_order_record (value,order_num,uid,addr,time)
       VALUES
          (#{value},#{order_num},#{userid},#{addr},#{time})
   </insert>

</mapper>    